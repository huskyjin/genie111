const fs = require(" fs\); const logPath = \C:/Users/user/.gemini/antigravity/brain/7382431b-0d1f-4ccb-b700-14a73cf63161/.system_generated/logs/transcript_full.jsonl\; const logContent = fs.readFileSync(logPath, \utf8\); const lines = logContent.split(\\n\); const ranges = [{start: 1, end: 800}, {start: 801, end: 1600}, {start: 1601, end: 2400}, {start: 2401, end: 3200}, {start: 3201, end: 3268}]; let finalCodeLines = []; for (const r of ranges) { const patt = \Showing lines \ + r.start + " to \ + r.end; let found = false; for (let i = lines.length - 1; i >= 0; i--) { const line = lines[i]; if (line.includes(patt)) { const json = JSON.parse(line); const content = json.content; const codeLines = content.split(\\n\); const chunkLines = []; for (const cl of codeLines) { const match = cl.match(/^\d+: (.*)$/); if (match) { chunkLines.push(match[1]); } } finalCodeLines.push(...chunkLines); found = true; console.log(\Found range \ + r.start + \-\ + r.end + " in step \ + json.step_index); break; } } if (!found) { console.warn(\WARNING: Could not find range \ + r.start + \-\ + r.end); } } if (finalCodeLines.length > 100) { const restoredCode = finalCodeLines.join(\\n\); fs.writeFileSync(\main.js\, restoredCode, \utf8\); console.log(\Success! main.js has been fully restored.\); } else { console.error(\Failed to restore main.js.\); }
